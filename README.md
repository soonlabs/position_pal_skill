# Position Safety Monitor Skill

This skill lets an agent request a position safety report using a Binance read-only API key.

## Usage
- Provide the API key and secret with read permissions only.
- Optional inputs control analysis depth and whether macro data is included.
